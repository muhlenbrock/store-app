import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
    imageContainer: {
      paddingTop: 30,
      height: 300,
      justifyContent: 'center',
      alignItems: 'center'
    },
    image: {
      width: '80%',
      height: '80%'
    }
  });